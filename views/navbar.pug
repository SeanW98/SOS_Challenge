include layout

doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/SOS_CSS.css')
    script(src='/javascripts/buttons.js')
    
block content
    body(onload ="buttonDisplay()") 
    ul(class="navbar")
        img(src="/images/logo.png" alt="logo" style="width:4%;height:4%;")

        lia(class="navbar")
            a(href="/") Home
        lia(class="navbar")
            a(href="/myAccount") My profile
        lia(class="navbar")
            a(href="/aboutUs") About us
        lia(class="navbar")
            a(href="/SignIn" id="signIn") Sign in
        lia(class="navbar")
            a(href="/logout" id="logout") Logout