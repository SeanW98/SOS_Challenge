include navbar

doctype html
html
  head
    title= title
block content
    h1 #{type}
    ul
        each business, i in businesses
            a(href= "/businesses/" + type + "/" + business._id , class='tourismList' )= business.businessName
include footer

// Parsing information found from https://www.youtube.com/watch?v=Ad2ngx6CT0M